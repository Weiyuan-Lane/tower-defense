{
  "gameSettings": {
    "maxWaves": 100,
    "wavePreparationTime": 60,
    "bossWaveFrequency": 5
  },
  "towerSettings": {
    "upgradeFormulas": {
      "cost": {
        "formula": "baseCost * Math.pow(1.5, level)",
        "description": "Tower cost increases by 50% for each level"
      },
      "damage": {
        "formula": "baseDamage * Math.pow(1.2, level)",
        "description": "Tower damage increases by 20% for each level"
      },
      "range": {
        "formula": "baseRange * Math.pow(1.1, level)",
        "description": "Tower range increases by 10% for each level"
      },
      "speed": {
        "formula": "baseSpeed * Math.pow(1.15, level)",
        "description": "Tower attack speed increases by 15% for each level"
      },
      "effectPower": {
        "formula": "baseEffectPower * Math.pow(1.25, level)",
        "description": "Tower effect power increases by 25% for each level"
      },
      "effectDuration": {
        "formula": "baseEffectDuration * Math.pow(1.15, level)",
        "description": "Tower effect duration increases by 15% for each level"
      }
    }
  },
  "enemySettings": {
    "scaling": {
      "health": {
        "formula": "baseHealth * Math.pow(1.1, wave)",
        "description": "Enemy health increases by 10% for each wave"
      },
      "speed": {
        "formula": "baseSpeed * Math.pow(1.05, wave)",
        "description": "Enemy speed increases by 5% for each wave"
      },
      "reward": {
        "formula": "baseReward * Math.pow(1.08, wave)",
        "description": "Enemy reward increases by 8% for each wave"
      }
    },
    "bossScaling": {
      "health": {
        "formula": "baseHealth * Math.pow(1.5, wave / 5)",
        "description": "Boss health increases by 50% for each boss wave"
      },
      "speed": {
        "formula": "baseSpeed * Math.pow(1.1, wave / 5)",
        "description": "Boss speed increases by 10% for each boss wave"
      },
      "reward": {
        "formula": "baseReward * Math.pow(1.3, wave / 5)",
        "description": "Boss reward increases by 30% for each boss wave"
      }
    }
  }
}
